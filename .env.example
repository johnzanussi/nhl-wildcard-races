# Vercel deploy hook URL — used by scripts/track-games.ts to trigger a
# production rebuild when a tracked game completes.
# Generate a new hook at: Vercel Dashboard → Project → Settings → Git → Deploy Hooks
VERCEL_DEPLOY_HOOK=https://api.vercel.com/v1/integrations/deploy/<project-id>/<hook-id>
