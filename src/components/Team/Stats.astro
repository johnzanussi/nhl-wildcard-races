---
type Data = {
    label: string;
    stat: string | number;
};

type Props = {
    data: Data[];
};

const {
    data,
} = Astro.props;
---

<div class="flex gap-1 sm:gap-4">
    {data.map(({ label, stat }) => {
        return (
            <div class="text-center max-w-10 sm:max-w-15">
                <div class="text-xs sm:text-base dark:text-slate-200">
                    {stat}
                </div>
                <div class="text-[8px]/[10px] sm:text-xs dark:text-slate-500">
                    {label}
                </div>
            </div>
        );
    })}
</div>
