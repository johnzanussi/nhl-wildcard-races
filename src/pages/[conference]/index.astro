---
import PageStandings from '@/components/PageStandings.astro';

export function getStaticPaths() {
    const conferences = ['east', 'west'];
    return conferences.map(conference => ({
        params: {
            conference
        },
    }));
}
---

<PageStandings />
